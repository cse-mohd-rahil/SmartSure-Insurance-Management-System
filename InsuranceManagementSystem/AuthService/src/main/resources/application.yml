server:
  port: 8081

spring:
  application:
    name: AuthService

  datasource:
    url: jdbc:mysql://localhost:3306/auth_db
    username: root
    password: Root@123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/